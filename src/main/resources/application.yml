spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: postgres
    url: jdbc:postgresql://localhost:5432/kakeibodb
    username: postgres

  messages:
    basename: ValidationMessages
    
  security:
    user:
      password: password
      username: user
    admin:
      password: password
      username: admin